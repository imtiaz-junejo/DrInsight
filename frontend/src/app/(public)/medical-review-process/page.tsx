import { MedicalReviewProcessContent } from "./MedicalReviewProcessContent";

export const metadata = {
  title: "Medical Review Process — How Content Is Verified | DrInsight",
  description:
    "Every DrInsight article passes a multi-stage medical review by board-certified specialists — editorial review, fact-checking and physician sign-off.",
};

export default function MedicalReviewProcessPage() {
  return <MedicalReviewProcessContent />;
}
