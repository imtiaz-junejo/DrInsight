export const metadata = {
  title: "Contact Us — MedAuthority",
  description:
    "Get in touch with MedAuthority — phone, email, WhatsApp, live chat, or send us a message. Our team responds within 2–4 hours.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
