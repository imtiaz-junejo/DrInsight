export const metadata = {
  title: "Medical Blog — MedAuthority",
  description:
    "1,000+ peer-reviewed medical articles across every specialty — written and reviewed by board-certified physicians.",
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
