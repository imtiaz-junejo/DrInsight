export const metadata = {
  title: "Medical Blog — DrInsight",
  description:
    "Peer-reviewed medical articles across every specialty — written and reviewed by board-certified physicians.",
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
