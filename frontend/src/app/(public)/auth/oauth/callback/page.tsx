import { OAuthCallbackPageContent } from "@/components/auth/OAuthCallbackContent";

export const metadata = {
  title: "Signing In — DrInsight",
  description: "Completing social sign-in.",
};

export default function OAuthCallbackPage() {
  return <OAuthCallbackPageContent />;
}
